# URL base del servidor Xipe (carpeta /server).
# Para desarrollo local con `npm run dev` del servidor:
VITE_API_URL="http://localhost:4000"

# Para producción apunta al dominio donde despliegues el servidor
# (Render, Railway, Fly, etc.):
# VITE_API_URL="https://api.tu-dominio.com"
